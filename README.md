# WORD RUSH — Game 1 technical prototype

Open `index.html` in a modern browser, or serve this directory with any static web server.

Controls:

- Left / Right Arrow: change lane
- Up Arrow / Space: jump

The current build is intentionally limited to the continuous pseudo-3D runner prototype described in the task. It does not include menus, grammar, results, persistence, or final audio/UI systems.
